code = 'test test (really test) wow'
def appendToken(tokens, t, depth):
    ls = 'tokens[-1]'
    for i in range(depth)
    return tokens

def filledTokenArray(tokens, x, y, size):
    tokens


def lexer(code):
    tokens = []
    tokenstack = ''
    depth = 1
    for i, t in enumerate(code):
        if t == ' ':
            pass





print(appendToken([], 'token', 10))